# Changelog

## 0.1.0 — Alpha

- isolated Git worktree creation and lifecycle management;
- persistent task state and transactional path leases;
- provider-neutral local worker execution with evidence artifacts;
- fail-closed completion validation with required checks;
- public `task run` orchestration and offline deterministic demo workflow;
- explicit cleanup and conservative recovery;
- Windows/Ubuntu CI configuration for Python 3.11 and 3.12.
